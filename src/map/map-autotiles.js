export default function autotiles (sets, layer) {
	// const autoTileLayers = [];
	// const autoTilesets = data.properties.autoTilesets.map(load);
	// TODO cache auto tile layers, one image for each frame of potential animation
}